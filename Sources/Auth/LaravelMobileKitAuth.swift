import LaravelMobileKitCore

/// Authentication capability: credentials, Keychain storage, token providers,
/// authenticators, and auth-state management.
public enum LaravelMobileKitAuth {
    /// Semantic version of the Auth module.
    public static let version = LaravelMobileKitCore.version
}
